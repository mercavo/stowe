<%% contents = []
   contents << "<p>Pode confirmar o email da sua conta através do link abaixo:</p>"
   contents << link_to("CONFIRME MINHA CONTA", confirmation_url(@resource, confirmation_token: @token)) %>
<%%= render("template", message: contents.join.html_safe) %>
